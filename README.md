-Developed a precise digital clock system in SystemVerilog to track minutes, seconds, and
milliseconds, employing custom frequency dividers to achieve an accurate 1ms resolution
from the base clock.

-Implemented both behavioral and structural architectures, designing multi-digit 7-
segment display decoders and complex enable-logic for sequential digit rollover.

-Utilized simulation optimization techniques by dynamically scaling time units to efficiently
verify long-term counter behavior and state transitions without excessive computation
time.
